{
  "name": "home-office-root",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd backend && npm install && npm install sqlite3 --build-from-source && (npx tsc --skipLibCheck || true) && cd ../frontend && npm install && npm run build",
    "start": "cd backend && node dist/index.js"
  }
}
