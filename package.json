{
  "name": "home-office-root",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd backend && npm install && rm -rf dist && npx tsc && cd ../frontend && npm install && npm run build",
    "start": "cd backend && node dist/index.js"
  }
}
